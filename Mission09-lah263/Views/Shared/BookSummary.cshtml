@{
}

@model Book

    <div class="card card-outline-primary m-1 p-1">
        <div class="bg-faded p-1">
            <h4>
                @Model.Title
                <!--ADDED INLINE TEXT COLOR FOR ONE CSS ATTRIBUTE-->
                <div class="card-text p-1" style="float:right; color:darkblue;">
                    @Model.Price.ToString("c")
                </div>
            </h4>
        </div>
        <div class="card-text p-1">@Model.Author</div>
        <div class="card-text p-1">@Model.Publisher</div>
        <div class="card-text p-1">@Model.Classification</div>
        <div class="card-text p-1">@Model.Category</div>
        <div class="card-text p-1">@Model.PageCount pages</div>
        <div class="card-text p-1">@Model.Isbn</div>
        <form method="post" asp-page="/Buy">
            <input type="hidden" asp-for="@Model.BookId" />
            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />

            <span class="card-text">
                <!--OUTLINED BUY BUTTON FOR SECOND CSS ATTRIBUTE-->
                <div class="d-grid p-3"><button type="submit" class="btn btn-outline-success">Buy</button></div>
            </span>
        </form>
    </div>

<br />